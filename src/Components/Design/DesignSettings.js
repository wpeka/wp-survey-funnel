import React, { useContext } from 'react';
import { DesignContext } from '../Context/DesignContext';

export default function DesignSettings() {
	
	const { fontColor, backgroundColor } = useContext( DesignContext );

	return (
		<div>
			
		</div>
	)
}
